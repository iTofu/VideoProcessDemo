# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VideoProcessDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VideoProcessDemo
  pod 'GPUImage'
  pod 'Then'

  target 'VideoProcessDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VideoProcessDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
